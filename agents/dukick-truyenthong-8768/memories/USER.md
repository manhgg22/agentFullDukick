When asked for plans, frameworks, or strategy documents, Leo🌷 prefers condensed, high-signal output first — headings, tables, bullet points. Detailed explanations should be stripped unless explicitly requested.
§
Leo🌷 (Duck Mẹn) communicates in very casual Vietnamese with short, direct imperatives. Expects me to act immediately on existing infrastructure/code they already have in place. When they mention code/files exist, ask for path right away rather than searching exhaustively.
§
User prefers Vietnamese in casual Dukick team context. Very direct, action-oriented, dislikes preamble — wants "setup ngay" not explanations. When asking for Drive links wants clickable URLs only, no extra text.
§
User configures Zalo Bot via mobile app console UI (screenshot showed phone screen). When something doesn't work, user expects immediate diagnosis — check logs first, don't make them re-describe.
§
When testing Zalo Bot/webhook server, user prefers NOT to kill the running process. Instead, test against the live server directly (curl to localhost or public URL) without restart.