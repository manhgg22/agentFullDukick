{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "shell": "powershell",
            "command": "$md = Get-Content 'C:\\DukickAgent\\CLAUDE.md' -Raw -Encoding utf8 -ErrorAction SilentlyContinue; if ($md) { @{ hookSpecificOutput = @{ hookEventName = 'PreToolUse'; additionalContext = \"=== CLAUDE.md (Dukick context) ===`n$md`n=== end CLAUDE.md ===\"} } | ConvertTo-Json -Compress } else { '{}' }",
            "timeout": 5,
            "statusMessage": "Loading CLAUDE.md context..."
          }
        ]
      }
    ]
  }
}
